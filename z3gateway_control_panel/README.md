# Z3Gateway Control Panel Add-on

Runs the local Z3Gateway Control Panel and the ARM64 `zigbee_z3_gateway` host binary inside Home Assistant OS.

This first package is for validating the HAOS path on `aarch64` only.
